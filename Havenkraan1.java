import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Havenkraan1 here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Havenkraan1 extends Kraan
{
    /**
     * Act - do whatever the Havenkraan1 wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
        //move(0);// dit willen we wellicht niet, dus event weer ongedaan maken (geen besturing kraan)
        /**if (Greenfoot.isKeyDown("up")){

            int x = getX();
            int y=  getY();
            int ny = getY()-1;

            setLocation(x, ny);

        }
        if (Greenfoot.isKeyDown("down")){

            int x = getX();
            int y = getY();
            int ny = getY()+1;

            setLocation (x, ny);
        }  
        */
    }     
}
